# подключение образа node
FROM node:18-alpine

# Создаем рабочую директорию для сервера
WORKDIR /app

# Копируем зависимости в докер
COPY package*.json /app/

# Установка зависимостей
RUN npm install

# Копируем все файлы программы
COPY . .

# Указываем порт, который будет использоваться в контейнере
EXPOSE 8080

# Команда для запуска приложения
CMD ["node", "server-v-1.5.js"]
